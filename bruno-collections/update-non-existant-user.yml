info:
  name: update-non-existant-user
  type: http
  seq: 13

http:
  method: PUT
  url: "{{baseUrl}}/users/69e490b1673fec54eaba724e"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "displayName": "{{$randomFullName}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        if (body && body.id) {
          bru.setVar("lastCreatedUserId", body.id);
          bru.setVar("lastCreatedUser", body);

          // Known Issue: String starting with "+" is treated as Number
          bru.getVar("lastCreatedUser").phoneNumber = String(bru.getVar("lastCreatedUser").phoneNumber);
          console.log("Captured userId: ", body.id);
        }
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
    - expression: res.body.error
      operator: eq
      value: User not found

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
