info:
  name: register-user-with-invalid-password-with-no-special-char
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/register"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "username": "{{$randomUserName}}",
        "email": "{{$randomEmail}}",
        "password": "1Ul35678121",
        "firstName": "{{$randomFirstName}}",
        "lastName": "{{$randomLastName}}",
        "phoneNumber": "{{BDPhoneNumber}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        if (body && body.id) {
          bru.setVar("lastCreatedUserId", body.id);
          bru.setVar("lastCreatedUser", body);

          // Known Issue: String starting with "+" is treated as Number
          bru.getVar("lastCreatedUser").phoneNumber = String(bru.getVar("lastCreatedUser").phoneNumber);
          console.log("Captured userId: ", body.id);
        }
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
    - expression: res.body.error
      operator: eq
      value: Password must contain uppercase, lowercase, digit, and special character

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
