info:
  name: register-user-with-only-required-field
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/register"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "username": "{{$randomUserName}}",
        "email": "{{$randomEmail}}",
        "password": "Strong@Password1",
        "firstName": "{{$randomFirstName}}",
        "lastName": "{{$randomLastName}}",
        "phoneNumber": "{{BDPhoneNumber}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        if (body && body.id) {
          bru.setVar("lastCreatedUserId", body.id);
          bru.setVar("lastCreatedUser", body);

          // Known Issue: String starting with "+" is treated as Number
          bru.getVar("lastCreatedUser").phoneNumber = String(bru.getVar("lastCreatedUser").phoneNumber);
          console.log("Captured userId: ", body.id);
        }
  assertions:
    - expression: res.status
      operator: eq
      value: "201"
    - expression: res.body.displayName
      operator: isNull
    - expression: res.body.dateOfBirth
      operator: isNull

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
