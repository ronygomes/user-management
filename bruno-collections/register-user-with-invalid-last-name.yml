info:
  name: register-user-with-invalid-last-name
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/register"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "username": "{{$randomUserName}}",
        "email": "{{$randomEmail}}",
        "password": "Strong@Password1",
        "lastName": "__def__",
        "firstName": "{{$randomLastName}}",
        "phoneNumber": "{{BDPhoneNumber}}",
        "dateOfBirth": "2005-06-11"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        if (body && body.id) {
          bru.setVar("lastCreatedUserId", body.id);
          bru.setVar("lastCreatedUser", body);

          // Known Issue: String starting with "+" is treated as Number
          bru.getVar("lastCreatedUser").phoneNumber = String(bru.getVar("lastCreatedUser").phoneNumber);
          console.log("Captured userId: ", body.id);
        }
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
    - expression: res.body.error
      operator: eq
      value: Invalid First Name or Last Name format

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
